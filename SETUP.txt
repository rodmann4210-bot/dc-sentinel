# DC Sentinel — Environment Variables
# Copy this to .env.local for local development
# Add each of these in Vercel Dashboard → Settings → Environment Variables

# ── REQUIRED ────────────────────────────────────────────────────
# Your Anthropic API key (powers TFR lookup, hotel, car rental, AI analysis)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ── OPENSKY (for aircraft data) ─────────────────────────────────
# Your OpenSky Network account credentials
OPENSKY_USER=your-opensky-username
OPENSKY_PASS=your-opensky-password

# ── ALERTS ──────────────────────────────────────────────────────
# Email address to receive alerts (yours)
ALERT_EMAIL=rodmann4210@gmail.com

# Phone number for SMS alerts (include country code, e.g. +12345678900)
ALERT_PHONE=+1YOUR10DIGITNUMBER

# ── EMAIL via Resend (free — 3,000 emails/month) ─────────────────
# Sign up at resend.com — free, takes 2 minutes
RESEND_API_KEY=re_your-resend-key-here

# ── SMS via Twilio (optional — ~$0.01/message) ───────────────────
# Sign up at twilio.com — free trial credit included
TWILIO_ACCOUNT_SID=ACyour-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_FROM_NUMBER=+1YOUR-TWILIO-NUMBER

# ── WEB PUSH (optional — completely free) ────────────────────────
# Generate VAPID keys by running: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# ── SYSTEM ───────────────────────────────────────────────────────
# Secret to protect the /api/collect endpoint
# Generate any random string: e.g. openssl rand -hex 32
CRON_SECRET=generate-a-random-secret-here

# Your Vercel app URL (filled in automatically after first deploy)
APP_URL=https://your-site-name.vercel.app

# ── VERCEL KV (auto-filled by Vercel after you add KV storage) ───
# These are added automatically when you connect KV in Vercel dashboard
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
